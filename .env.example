# Google Maps SDK + Places/Geocoding key (Android & iOS)
# Local dev: copy this file to .env and fill in your key
# CI/CD: use EAS Secrets → npx eas secret:create --scope project --name GOOGLE_MAPS_API_KEY --value <key>
GOOGLE_MAPS_API_KEY=
